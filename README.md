# GestionCentraleUCL

This repository contains a lightweight Python toolkit that models the core
features needed to manage a student association of around 150 members.  It
implements member management, role‑based access control and basic event
planning inspired by the ideas outlined in ``instruction.pdf``.

## Development

```bash
pip install -r requirements.txt
pytest
```
